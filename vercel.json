{
  "buildCommand": "npx ng build --configuration production --base-href /",
  "outputDirectory": "dist/ng-matero/browser",
  "installCommand": "yarn install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
